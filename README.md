# hello friends
- this is the worlds only terminal that will have an agent called the termitoria
- this will have a reader which will read outputs of the cmd's you will run in it
- the weapons, targets, agent and the reader toggling btn which alows you to turn the reader on and of
- it's made of rs and sh
- and a red and green programming vibe made with css
## live demo =>
https://termirator-j795.onrender.com/
## it allows two special cmd's: set mem;, set window;
### set mem=VAlue = start a recorder that will record every cmd you run and tabs you open and name then go back to the recorder tab and RUN KEY: CTRL+C; to stop the recorder
then whenever you restart the *web app* (termirator) run set mem to set all tabs and their names, agent mem and cmd's
## same to set window=VALUE and set window
